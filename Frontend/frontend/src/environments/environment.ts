export const environment = {
  production: false,
  // Relative path — the dev-server proxy (proxy.conf.js) forwards /api to the
  // backend endpoint that Aspire injects. Keeps the browser same-origin.
  apiBaseUrl: '/api',
};
